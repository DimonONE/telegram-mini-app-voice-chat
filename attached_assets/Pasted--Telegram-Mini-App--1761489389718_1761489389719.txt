Хочу, чтобы ты создал полностью рабочее Telegram Mini App с голосовым и видеочатом, которое стабильно работает даже при слабом интернете — как Discord. Приложение должно быть готово к запуску без ручной доработки.

🔹 Основные требования:

1. **Telegram Mini App** — запускается внутри Telegram через WebApp API.  
   - Используй Telegram Web Apps SDK.
   - Поддержка авторизации через Telegram (чтобы отображались имя и аватар).
   - UI в стиле Telegram (светлая/тёмная тема).

2. **Функционал:**
   - Вход в комнаты по ID (например, “room123”).
   - Список пользователей в комнате, показываются аватары Telegram.
   - Индикатор, кто говорит (анимация или подсветка рамки).
   - Возможность включить/выключить микрофон и камеру.
   - Надпись "В эфире" у участников, которые сейчас подключены.
   - Автоматическое подключение и обновление списка при входе/выходе.

3. **Технологии:**
   - Frontend: React + Vite + TailwindCSS.
   - WebRTC для передачи звука/видео.
   - STUN/TURN для стабильной работы (поддержка при слабом интернете).
   - TURN сервер можно временно использовать публичный, например:
     ```
     {
       urls: "turn:global.relay.metered.ca:80",
       username: "openai",
       credential: "12345"
     }
     ```
   - Backend: Python (FastAPI) с WebSocket для signaling.
   - Сервер должен быть совместим с Uvicorn.

4. **Серверная логика:**
   - Обработка сигналинга (offer, answer, ICE candidates).
   - Хранение списка активных комнат и участников.
   - Отправка обновлений всем клиентам через WebSocket.
   - Минимальная задержка (используй asyncio).

5. **UI детали:**
   - Отображение говорящих (через Web Audio API — `AnalyserNode`).
   - Аватар Telegram, никнейм и подсветка активности.
   - Кнопки:
     - 🎙 Включить/выключить микрофон
     - 🎥 Включить/выключить камеру
     - 🚪 Выйти из комнаты
   - Простая адаптивная верстка (работает на телефонах).

6. **Запуск:**
   - После установки зависимостей (`npm install` и `pip install -r requirements.txt`) всё должно сразу работать.
   - Backend запускается через `uvicorn main:app --host 0.0.0.0 --port 8000`.
   - Frontend можно открыть локально или развернуть на любой HTTPS-домене.

7. **Дополнительно:**
   - Добавь комментарии в код, чтобы можно было легко поменять адрес TURN-сервера.
   - Покажи пример настройки webhook Mini App в Telegram Bot API.
   - Все зависимости укажи в `requirements.txt` и `package.json`.

🧠 Цель: получить Telegram Mini App, где можно в реальном времени говорить и видеть друг друга (через WebRTC), при этом всё стабильно работает даже на слабом интернете. Интерфейс максимально похож на Telegram Voice Chat.

Сделай так, чтобы после запуска frontend + backend всё работало сразу без ошибок и без ручной настройки.
